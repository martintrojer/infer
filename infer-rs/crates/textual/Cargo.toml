[package]
name = "textual"
description = "Textual IR parser and printer for the Infer static analyzer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sil = { path = "../sil" }
serde = { workspace = true }
num-bigint = { workspace = true }
num-traits = { workspace = true }
logos = "0.15"

[dev-dependencies]
insta = "1"
